import { Box } from "@mui/material";
import React from "react";

const ImageEnlargedModal = () => {
  // Generate a component that displays the image in a Modal component.
  return <Box></Box>;
};

export default ImageEnlargedModal;
